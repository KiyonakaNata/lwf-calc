# 使い方ガイド

[← README](README.md) ／ ツールを開く: <https://kiyonakanata.github.io/lwf-calc/>

**「このスポンサー構成で、どの契約とレシピを組み合わせると強いか」を先に机上で確かめる**ためのツールです。
ゲームの1シフトを再現するものではありません（時間・返済・採掘の計算は [v020/](v020/)＝返済シミュ版へ）。

## 基本の流れ

1. **開発後援を選ぶ**（左パネル上部のチップ・3体まで＋ゲヘナ常時）
   - 選んだ悪魔の契約・ポータル・看板・解禁レシピだけが出ます。
   - 外すとその悪魔の契約の取得はすべて0に戻ります（そのシフトでは取れないため）。
2. **契約を取る**（真ん中の契約一覧、または📇タブの「取得」ボタン）
   - 全レシピの数値が一斉に補正後の値へ変わり、変わった行は青くなります。
   - 再利用不可の契約はチップ（取ると ✓ が付き、もう一度押すと取り消し）、重ねがけできる契約は回数ステッパです。
   - 取った契約は**レシピ計算側の「取得済み」**に取得順で並びます。
3. **レリック・バフ・ポータルを設定**（左パネル下段）
   - 認定証・リトマス紙・酸性触媒などは所持数を入力（契約ぶんは自動加算）。
   - 猛毒・分別不要・タイムウォークは**実プレイでは時限バフ**ですが、ここでは常時ONのトグルとして効果を確かめられます。
   - ポータルは回数＝納品した数。納品するたび効果が重なります。
4. **🎁返済特典**（計算に効くものだけ収録）
   - レシピ改変（脱水率の引き上げ・制限温度の廃止）、レイヴンの1秒の再定義、ピッケルの握り方講座、
     ゲヘナの契約を解放する2種（特別高額納税者＝視線／模範労働者＝流通掌握）。
   - **専属**＝その悪魔を開発後援に入れたシフトでしか発動しません（後援に居ないとグレーで選べません）。

## 取得順（取得済み）

契約の補正は**上から順に逐次計算**します。同じ項目への掛け算と足し算は順番で結果が変わるので、
「水冷クラフト → オーバーヒート → 水冷クラフト」のような手順もそのまま組めます。

- **−** その位置の取得を1回ぶん解除 ／ **＋** その位置でもう1回取得 ／ **▲▼** 並べ替え。
- 同じ契約は離れた位置に何度でも置けます（連続するぶんは「×2」とまとめて表示）。
- **圧縮・希釈は「取ったときに解禁済みのレシピ」にしか掛かりません**（ゲームの実装どおり）。
  解禁より前に取った場合、そのレシピの行に ⏳ が付きます。取得済み欄で解禁契約より後ろへ動かすと乗ります。

## 📋 レシピ計算タブ

### Σ集計（配置した台数ぶんの合計）

各レシピ行の「配置」に台数を入れると、上の集計に合算されます。

- 見出し … **現金/s**・化学反応/s・刻印バフの現金化（刻印コバルト／ルビーはクラフトのたび1個消費して現金+1）。
- **資源の差分** … 7資源の増減/s。プラスは余り、マイナスは外から用意する量です（余ったアイテムの納品売却ぶんも計上）。
- **⚠不足（中間）** … クラフトでしか作れない材料の不足。**🔗不足で下流を絞る**をONにすると、
  足りない材料を使うレシピの稼働をその割合まで自動で落とします（行に ⚠×0.5 のように出ます）。
- **詳細** … アイテムごとの生産・消費・余剰、外から用意する原料（**必要な採掘ゴブリンの目安つき**）、配置の合計台数。

### レシピの表

- 使い魔ごとの表で、レシピ / 配置 / 個数 / 製造時間 / 個/s / 材料 / 買取 / 差引 を並べます。
- 契約で変わった値は「**補正後 ×倍率**」で表示（基礎値はホバー）。
- **🔓 解禁契約** … 各使い魔の表の上のチップ。ONにするとそのレシピの行が出ます。
- **依頼看板のレシピ**（143件）は一期一会なので既定オフ。表の上のピッカーで引けたもの・検討したいものだけ足せます。
- 表示の切り替え（表の上のチェックボックス）
  - **材料・買取を /s で表示** … 1回あたりの個数 ↔ 毎秒の消費量・取得量。
  - **配置数をかけて合計で表示** … 1台あたり ↔ 配置した台数ぶんの合計。

### 運搬

- **運搬上限**（水流10/s・激流40/s）を超える値は橙・赤で警告。浚渫・インフラ投資で上限が伸び、
  **水冷クラフト・丁寧に作れの運搬速度低下**（1ptにつき−1%）で下がります。
- **運送レイヴン**は最遅（移動範囲いっぱいの距離）の運搬量を表示します。
  マモンの契約（「1」の再定義・地獄改造論）と返済特典「1秒の再定義」で速くなります。

## 📇 契約・注文・看板タブ

選んだ悪魔ごとに「その悪魔から手に入るもの」を並べます。

- **契約** — 全契約の効果一覧。計算に反映されるものはその場の「＋」で取得できます（契約一覧と連動）。
  レシピの数値を変えない契約は分類タグ（杖・入手・経済・採掘・時限）だけ付けて効果文を載せています。
- **注文** — 納品すると報酬がもらえる注文のテーブル（Lv・納品・報酬）。
  時限バフ（ゴールドラッシュ・猛毒・タイムウォークなど）の入手先もここで分かります。
- **依頼看板** — 看板でもらえる資材・召喚物・契約の一覧。**要求Lv**は看板が出る距離の目安
  （初期地点から 1=近い 〜 4=遠い）。レシピ看板の中身は📋タブのピッカーから足して確かめてください。

## 計算のルール（要点）

- 同一項目への倍率は**すべて掛け算**、固定加減算は回数倍。取得順に上から逐次適用します。
- **使い魔の速度バフは加算**: 猛毒 +300%・トレーニングジム +25%/回・24時間シフト +200%・
  地下水凝固 +0.5%/個 などは足し合わせてから 所要時間 ÷ (1+合計) になります。
  重ねるほど1個あたりの効きは鈍ります（×4×8=×32 のような掛け算にはなりません）。
- 圧縮・丁寧に作れなどの「製造時間 xN」はレシピ側の補正＝掛け算です。
- 余ったアイテムは納品売却したものとして資源の差分に計上します（中間材料としての消費が優先）。

## 対象外

返済ノルマ・タイムライン・採掘収入のシミュレーション（→ [v020/](v020/)）、杖スキル、
土地・注文の実行、環境汚染や前科などのペナルティ通貨は計算しません。
