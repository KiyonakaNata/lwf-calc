# 使い方

[← README](README.md) ／ 計算機本体: <https://kiyonakanata.github.io/lwf-calc/>

画面は **左＝設定（固定）／右＝2つのタブ** です。
左で契約・レリック・バフを設定し、右のタブで結果を見ます。

- **契約スキル**: 取得回数を入力（ver 0.20 でレア以上は1回のみ＝ON/OFFトグル）。`適用順` の ▲▼ で計算順を変えられます（同じ項目への倍率は掛け算なので、順番で結果が変わります）。
- **レリック・バフ**: 認定証・リトマス紙・酸性触媒の所持数と、分別不要・猛毒の有効/無効。契約ぶんに注文/手動入手を足せますが、**注文/手動ぶんは返済の進行度に数えません**。

---

## レシピ計算タブ

全レシピを使い魔ごとのカードで表示します。左の設定を変えると全部が一斉に更新されます。

- カード右上の **配置数** を変えると `/秒` の値が台数分になります。
- 生産速度が **運搬上限**（水流コンベア 10/s・激流コンベア 40/s）を超えると橙／赤で警告します（「水冷クラフト」を入れると上限が下がります）。

---

## 返済シミュレータータブ

難易度と契約から返済ノルマを出し、選んだレシピ・採掘の収入で**いつ返し終わるか**を計算します。
上端には **返済 / 返済額 / 必要時間 / 収入** のカードと、返済を進めるボタン・**📈詳細（別窓）** が貼り付いています。

### 収入をつくる
- **返済に使うレシピ**: 現金を生むレシピを ✔ して台数を入れると、合計の現金/秒が出ます。現金にならない中間レシピも「🧩材料用」として並びます。
- **中間素材は自動で回ります**: ✔したレシピの産物が別の✔レシピの材料なら、**材料へ優先的に充当し、余ったぶんだけ売却**します（行に「↪材料へ充当中」）。
- **搬出上限**: 全体（空欄＝無制限／プリセット 10・40）とレシピ個別。超えたぶんは生産の頭打ちとして収入に反映されます。
- **⛏ 採掘**: 「採掘収入」枠でゴブリン体数と砥石を入れると、クリティカル採掘の現金が収入に加わります。式は `体数 ×(採掘速度 ÷ 打数3)× クリ率 × 砥石所持数`。速度・クリ率は採掘系契約から自動計算します（砥石は ver 0.20 では1個が上限）。
  下の **「⛏土地ごとのゴブリン割当」** で土地ごとに割り当てると、原料の獲得/秒・原料収支・採掘現金がそのまま収支と在庫に反映されます（割当があるときは簡易値より優先）。

### 足りない材料を見つける
- レシピ行の **⚠材料不足** … その材料が「収支が赤字」「供給が自分の売却だけで外部から入ってこない」「在庫切れで律速中」のいずれか。
- 不足が出ると **⛏割当が自動で開き**、掘れば埋まる採掘物が色付きで示されます。
- **⛏ 不足の自動補填**: 不足ぶんのゴブリンをまとめて追加できます。材料そのものが掘れるならチェック、薬品・食料品などのタグ資源は「どのドロップを掘って売るか」を選ぶ方式です。

### 進行度と返済
- **進行度** = 取得した契約の数（ポータル・注文入手は除く）＋スキップ補正。これが「今何回目の返済か」になります。
- **🎁 返済特典**（難易度のすぐ下）: 「給付：研究開発費」で開始時に7資源が各 +20。**返済が始まると変更できません**（「初期化」で戻せます）。「追加融資」は開始後も変更できます。
- **返済の見通し（グラフ）**: 累計収入と返済ラインを表示。●にカーソルを合わせると、その回の返済額・収入/秒・現在位置からの所要が出ます。各回の数字は下の「返済表」に畳んであります。
- **⏰ 30分制限**: ゲームは30:00で終了するため、間に合わない返済には⏰が付きます。30:00より前に完済できる場合は「あと何回返せるか」も試算します。

### 📈 詳細（別窓）
在庫推移・収入の内訳（貢献度）・取得プランの編集・ステップ表を、**閉じるまで手前に残る小窓**で開きます（PiP対応ブラウザ）。
メイン画面の設定に連動してライブ更新されるので、設定を触りながら並べて見られます。

### 表示のルール（知っておくと迷いません）
数字は**2系統**あります。混ざっているわけではなく、用途で分けています。

| 系統 | どこ | 意味 |
|---|---|---|
| **実効値** | 収入カード・各レシピの現金/秒・📦生産・在庫・返済表 | 在庫切れによる律速を含んだ実際の値。📈別窓のグラフと必ず一致します |
| **設計値** | 資源収支・⛏不足の自動補填・余剰・必要な使い魔 | 選んだ台数が**フル稼働**したときの値。何がどれだけ足りないかを見るため |

- 律速が起きているときは 📦生産 の先頭に `📉在庫切れ律速: 資源 ×係数` が出ます。
- **在庫を持てるのは資源タグ（建材〜燃料・現金）だけ**です。アイテムはゲームに在庫の概念が無いため在庫欄に出ません（不足は赤字と⛏補填欄で確認します）。
- **1k表記**（既定ON・難易度の横で切替）: 1,000以上を `1k` / `1.23M` で短く表示します。

---

## 🎲 契約取得をゲーム再現（おまけ）

ONにすると「▶ 返済する（契約を選ぶ）」から、**ゲーム本体と同じ抽選規則**で契約を選ぶモードになります。
引き運込みでプランを試したいとき用です（OFFなら従来どおり手で契約を選べます）。

- 手札は **3枚＋常設の「選択スキップ」**。🎲リロールは3枚まとめて引き直し（初期15回／Hell以上は10回）。
- 出現率は進行度で変化（0〜2回目 コモン60%/アンコモン35%/レア5% … 12回目以降 30/35/17/16/2）。そのレアリティが山札に無ければ下位へフォールバックします。
- 「1回のみ」の契約は取ると山札から消えます。取った契約はそのまま左パネルに反映されます。
- 杖・日雇い・コンベアなど、この計算機が数値を持たない契約は **「ハズレ（生産に直接寄与しない）」** として進行度だけ進めます。
- ON中は**左パネルの契約と📈取得プランの編集がロック**されます（取得順＝抽選結果そのもののため）。やり直すときは「初期化」。
- デッキは体験版の4悪魔（ルシファー/レヴィアタン/サタン/ゲヘナ）55枚。使い魔の設置数などに制限はかけていません。

---

## データの編集

ページ下部の **「データを編集 / 確認（JSON）」** から、全アイテム・全スキルの値（生成個数・所要時間・材料・買取価格・スキル効果）を書き換えて「読み込む」で即反映できます。
効果の解釈が違うと思ったときの検証用です。編集はブラウザ内だけの一時的なもので、再読み込みで初期値に戻ります。

---

## 🎮 再現モード（旧版）

1ゲーム（30分）をまるごと再現して完済タイムを競うモードは、ver 0.19.0 準拠の
[v019/](https://kiyonakanata.github.io/lwf-calc/v019/) に凍結してあります。
使い方は **[v019/README.md](v019/README.md)** を参照してください。
